version: "3"
services:
  frontend-dev:
    container_name: frontend-dev
    build:
      context: ../
      dockerfile: dev/Dockerfile
    volumes:
      - ../public:/app/public
      - ../src:/app/src
    ports:
      - 3000:3000
